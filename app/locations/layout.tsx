import type { ReactNode } from "react";

import "@/styles/vbs-locations.scss";
import "@/styles/vbs-laptop-heading-scale.scss";

export default function LocationsLayout({ children }: { children: ReactNode }) {
  return children;
}
