import type { ReactNode } from "react";

import "bootstrap/dist/css/bootstrap.min.css";
import "@/styles/vbs-laptop-heading-scale.scss";

/** Pipeline Health Diagnostic — Bootstrap for LandingTopHeader (Container/Image). */
export default function PipelineHealthDiagnosticLayout({ children }: { children: ReactNode }) {
  return children;
}
