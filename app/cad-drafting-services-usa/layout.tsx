import type { ReactNode } from "react";

import "@/styles/vbs-cad-drafting-services-usa.scss";
import "@/styles/vbs-laptop-heading-scale.scss";

export default function CadDraftingServicesUsaLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
