import { GetInTouchSection } from "@/components/sections/get-in-touch-section";

/** Contact page — same Get in Touch block as site footer sections (Figma 2573:28324). */
export function ContactGetInTouchSection() {
  return (
    <GetInTouchSection
      sectionId="get-in-touch"
      className="vbs-contact-page-get-in-touch"
    />
  );
}
